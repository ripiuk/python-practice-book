"""
Problem:
    How many multiplications are performed when each of the following lines of code is executed?

    >>> def square(x):
    ...     return x * x

    print(square(5))
    print(square(2*5))

Solution:
    >>> print(square(5))  # one
    25
    >>> print(square(2*5))  # two
    100
"""
